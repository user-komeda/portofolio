---
interface Props {
  className?: string
}

const { className = '' } = Astro.props

const splitClasses = className.split(' ')
---

<div
  class="rounded-md w-full flex flex-col gap-2 bg-white dark:bg-gray-950/30"
  class:list={splitClasses}
>
  <slot />
</div>
